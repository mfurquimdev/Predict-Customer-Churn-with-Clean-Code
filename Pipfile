[[source]]
url = "https://${PYPI_USER}:${PYPI_PASS}@pypi.voltaware.com/simple"
verify_ssl = true

[packages]
pandas = ">=1.4.0"
sedss = ">=2.5.0"
mlda = ">=1.4.4"
geopy = ">=2.2.0"

[dev-packages]
pre-commit = "~=2.17.0"
black = "~=22.3.0"
pytest = "*"
coverage = ">=4.0.0"
pytest-cov = "*"
ipdb = "*"
pylint = "*"

[requires]
python_version = "3.8"

[scripts]
unittests = "pytest -s -vv --failed-first -x --cov-report term-missing --cov=. tests"
format = "black --line-length=120 --target-version=py38 --extend-exclude=.ipynb_checkpoints/|knowledge_thresholds.py ."
